// === Curated Questions (60) ===

var QUESTIONS = [
  {
    question:
      "In 2013, Dell issued a recall after customers complained their laptops smelled like <BLANK>.",
    answer: "cat urine",
    alternateSpellings: ["cat pee", "feline urine", "cat piss"],
    category: "recall",
    suggestions: [
      "cupcakes",
      "grandma",
      "burnt hair",
      "Cheez Whiz",
      "hummus",
      "rotten eggs",
      "dead fish",
      "body odor",
      "farts",
    ],
  },
  {
    question:
      "In October of 2013, eight sixth-graders from a New York college prep school were hospitalized after someone released <BLANK> in a classroom.",
    answer: "Axe body spray",
    alternateSpellings: ["Axe", "Axe spray", "body spray"],
    category: "school",
    suggestions: [
      "bees",
      "a snake",
      "tear gas",
      "pepper spray",
      "a skunk",
      "laughing gas",
      "glitter",
      "fire ants",
    ],
  },
  {
    question: "Kevin Spacey's older brother is a <BLANK> impersonator.",
    answer: "Rod Stewart",
    alternateSpellings: ["Rod Stewert"],
    category: "celebrity",
    suggestions: [
      "Abe Lincoln",
      "Cher",
      "Robin Williams",
      "Charlie Chaplin",
      "Kevin Spacey",
      "Barack Obama",
      "Elvis",
      "Sean Connery",
    ],
  },
  {
    question:
      "Under Peter the Great, noblemen had to pay 100 rubles a year for a <BLANK> license.",
    answer: "beard",
    alternateSpellings: ["beard license", "facial hair"],
    category: "history",
    suggestions: [
      "mustache",
      "wig",
      "horse",
      "sword",
      "drinking",
      "dancing",
      "hat",
      "carriage",
    ],
  },
  {
    question:
      "Amerigo Vespucci, the man for whom America was named, was a <BLANK> dealer.",
    answer: "pickle",
    alternateSpellings: ["pickles"],
    category: "history",
    suggestions: [
      "fur",
      "spice",
      "wine",
      "slave",
      "art",
      "tobacco",
      "silk",
      "weapon",
    ],
  },
  {
    question: "Frank Hayes is the first jockey to win a race while <BLANK>.",
    answer: "dead",
    alternateSpellings: ["deceased", "being dead"],
    category: "sports",
    suggestions: [
      "asleep",
      "blindfolded",
      "drunk",
      "backwards",
      "naked",
      "pregnant",
      "singing",
      "handcuffed",
    ],
  },
  {
    question:
      "In 2009, a woman in Brazil tried to buy a car using a bag full of <BLANK>.",
    answer: "hand-drawn money",
    alternateSpellings: [
      "fake money she drew",
      "drawn money",
      "hand drawn cash",
    ],
    category: "crime",
    suggestions: [
      "chicken nuggets",
      "lottery tickets",
      "teeth",
      "buttons",
      "bottle caps",
      "dead bugs",
      "marbles",
      "coupons",
    ],
  },
  {
    question: "In Thailand, it is illegal to step on <BLANK>.",
    answer: "money",
    alternateSpellings: ["currency", "Thai money", "baht", "coins"],
    category: "law",
    suggestions: [
      "a crack",
      "ants",
      "a shadow",
      "flowers",
      "someone's foot",
      "grass",
      "a grave",
      "a monk",
    ],
  },
  {
    question:
      "The city of Olney, Illinois organizes an annual event in order to <BLANK> squirrels.",
    answer: "count",
    alternateSpellings: ["census", "count the"],
    category: "animals",
    suggestions: [
      "race",
      "chase",
      "dress up",
      "baptize",
      "honor",
      "catapult",
      "wrestle",
      "photograph",
    ],
  },
  {
    question:
      "A 1999 interview with <i>US Weekly</i> revealed that actress Lucy Liu once claimed she had sex with a <BLANK>.",
    answer: "ghost",
    alternateSpellings: ["a ghost", "spirit"],
    category: "celebrity",
    suggestions: [
      "muppet",
      "robot",
      "vampire",
      "clown",
      "tree",
      "senator",
      "alien",
      "mime",
    ],
  },
  {
    question:
      "Walter Arnold received the world's first speeding ticket in 1896 for going <BLANK> miles per hour.",
    answer: "8",
    alternateSpellings: ["eight", "8 mph"],
    category: "history",
    suggestions: ["3", "12", "15", "20", "25", "6", "11", "4"],
  },
  {
    question:
      "Edgar Valdez Villarreal, a notorious Mexican drug cartel leader, had the not-so-scary nickname 'La <BLANK>.'",
    answer: "Barbie",
    alternateSpellings: ["barbie doll"],
    category: "crime",
    suggestions: [
      "Cupcake",
      "Butterfly",
      "Kitten",
      "Nana",
      "Princess",
      "Daisy",
      "Peanut",
      "Snowflake",
    ],
  },
  {
    question:
      "In 2014, a pub in England began offering a pizza topped with <BLANK>.",
    answer: "a whole rabbit",
    alternateSpellings: ["rabbit", "an entire rabbit"],
    category: "food",
    suggestions: [
      "chocolate",
      "a cheeseburger",
      "gold leaf",
      "gummy bears",
      "cotton candy",
      "a hot dog",
      "Froot Loops",
      "bugs",
    ],
  },
  {
    question: "The Super Soaker water gun was invented by a <BLANK> engineer.",
    answer: "NASA",
    alternateSpellings: ["a NASA", "rocket"],
    category: "inventions",
    suggestions: [
      "plumbing",
      "Disney",
      "military",
      "NERF",
      "janitor",
      "retired",
      "kindergarten",
      "prison",
    ],
  },
  {
    question:
      "IKEA is an acronym that includes the name of the <BLANK> where founder Ingvar Kamprad grew up.",
    answer: "farm",
    alternateSpellings: ["the farm", "a farm"],
    category: "business",
    suggestions: [
      "town",
      "street",
      "forest",
      "lake",
      "school",
      "church",
      "island",
      "mountain",
    ],
  },
  {
    question: "Oxford University is older than the <BLANK>.",
    answer: "Aztec Empire",
    alternateSpellings: ["Aztecs", "Aztec civilization"],
    category: "history",
    suggestions: [
      "printing press",
      "compass",
      "English language",
      "Eiffel Tower",
      "United States",
      "piano",
      "telescope",
      "bicycle",
    ],
  },
  {
    question: "Nintendo was originally a <BLANK> company.",
    answer: "playing card",
    alternateSpellings: ["playing cards", "card game", "card"],
    category: "business",
    suggestions: [
      "taxi cab",
      "rice",
      "fishing",
      "soap",
      "noodle",
      "umbrella",
      "furniture",
      "matchstick",
    ],
  },
  {
    question: "Honey is the only food that doesn't <BLANK>.",
    answer: "spoil",
    alternateSpellings: ["go bad", "rot", "expire", "decompose"],
    category: "food",
    suggestions: [
      "bounce",
      "freeze",
      "dissolve",
      "float",
      "shrink",
      "change color",
      "smell",
      "melt",
    ],
  },
  {
    question: "In Switzerland, it is illegal to own just one <BLANK>.",
    answer: "guinea pig",
    alternateSpellings: ["guinea pigs"],
    category: "law",
    suggestions: [
      "sock",
      "shoe",
      "glove",
      "goldfish",
      "ski",
      "chopstick",
      "earring",
      "child",
    ],
  },
  {
    question:
      "The average person spends 2 weeks of their lifetime waiting for <BLANK>.",
    answer: "traffic lights to change",
    alternateSpellings: ["traffic lights", "red lights", "stoplights"],
    category: "facts",
    suggestions: [
      "elevators",
      "the bus",
      "pizza delivery",
      "their spouse",
      "a doctor",
      "the microwave",
      "laundry",
      "a text back",
    ],
  },
  {
    question:
      "Before alarm clocks were invented, people hired a <BLANK> to wake them up.",
    answer: "knocker-upper",
    alternateSpellings: ["knocker upper", "person to knock on their window"],
    category: "history",
    suggestions: [
      "rooster trainer",
      "bell ringer",
      "town crier",
      "drummer",
      "screamer",
      "pebble thrower",
      "whistle blower",
      "neighbor",
    ],
  },
  {
    question: "Coca-Cola was originally <BLANK> colored.",
    answer: "green",
    alternateSpellings: ["green colored"],
    category: "food",
    suggestions: [
      "clear",
      "red",
      "yellow",
      "purple",
      "orange",
      "blue",
      "pink",
      "white",
    ],
  },
  {
    question: "The hashtag symbol (#) is technically called an <BLANK>.",
    answer: "octothorpe",
    alternateSpellings: ["octothorp"],
    category: "language",
    suggestions: [
      "hashtag",
      "pound sign",
      "number mark",
      "crosshatch",
      "gridline",
      "tic-tac-toe",
      "sharp sign",
      "fence post",
    ],
  },
  {
    question: "A group of flamingos is called a <BLANK>.",
    answer: "flamboyance",
    alternateSpellings: ["a flamboyance"],
    category: "animals",
    suggestions: [
      "flock",
      "parade",
      "pinkness",
      "flutter",
      "strut",
      "blush",
      "dazzle",
      "elegance",
    ],
  },
  {
    question: "The longest wedding veil ever was longer than <BLANK>.",
    answer: "63 football fields",
    alternateSpellings: ["63 football fields long", "almost 4 miles"],
    category: "records",
    suggestions: [
      "the Golden Gate Bridge",
      "10 city blocks",
      "a marathon",
      "the Titanic",
      "Mount Everest's base",
      "a Boeing 747",
      "5 miles",
      "the Nile River",
    ],
  },
  {
    question:
      "In Japan, there is a hotel that has been run entirely by <BLANK> since 2015.",
    answer: "robots",
    alternateSpellings: ["robot staff", "androids"],
    category: "technology",
    suggestions: [
      "cats",
      "monks",
      "children",
      "grandmothers",
      "ghosts",
      "AI",
      "clones",
      "volunteers",
    ],
  },
  {
    question: "The inventor of the Pringles can is <BLANK>.",
    answer: "buried in one",
    alternateSpellings: ["buried in a Pringles can", "interred in one"],
    category: "food",
    suggestions: [
      "a chemist",
      "also the inventor of Play-Doh",
      "allergic to potatoes",
      "legally blind",
      "a woman",
      "buried with one",
      "from Sweden",
      "anonymous",
    ],
  },
  {
    question: "In 2017, a man sued his parents for <BLANK>.",
    answer: "giving birth to him without his consent",
    alternateSpellings: [
      "being born without consent",
      "birthing him without consent",
    ],
    category: "law",
    suggestions: [
      "naming him Keith",
      "not buying him a pony",
      "throwing away his toys",
      "being too supportive",
      "making him eat vegetables",
      "being boring",
      "giving away his dog",
      "not letting him watch TV",
    ],
  },
  {
    question: "The shortest war in history lasted <BLANK> minutes.",
    answer: "38",
    alternateSpellings: ["38 minutes", "thirty-eight"],
    category: "history",
    suggestions: ["12", "45", "7", "90", "23", "3", "55", "17"],
  },
  {
    question: "Scotland's national animal is the <BLANK>.",
    answer: "unicorn",
    alternateSpellings: ["a unicorn"],
    category: "geography",
    suggestions: [
      "haggis",
      "highland cow",
      "Loch Ness monster",
      "thistle",
      "stag",
      "puffin",
      "dragon",
      "gryphon",
    ],
  },
  {
    question:
      "In 2016, a man in Florida was arrested for throwing <BLANK> at his neighbor.",
    answer: "an alligator",
    alternateSpellings: [
      "alligator",
      "a gator",
      "an alligator through a window",
    ],
    category: "florida",
    suggestions: [
      "a toilet",
      "his dentures",
      "a microwave",
      "a frozen turkey",
      "bees",
      "a snake",
      "a lawn gnome",
      "a birthday cake",
    ],
  },
  {
    question:
      "Cleopatra lived closer in time to the <BLANK> than to the building of the Great Pyramid.",
    answer: "Moon landing",
    alternateSpellings: ["moon landing", "first moon landing", "lunar landing"],
    category: "history",
    suggestions: [
      "iPhone",
      "printing press",
      "Industrial Revolution",
      "fall of Rome",
      "discovery of America",
      "Wright Brothers",
      "French Revolution",
      "birth of Jesus",
    ],
  },
  {
    question: "The original name for Pac-Man was <BLANK>.",
    answer: "Puck Man",
    alternateSpellings: ["Puck-Man", "Puckman"],
    category: "gaming",
    suggestions: [
      "Munch Man",
      "Chomp Boy",
      "Dot Eater",
      "Ghost Hunter",
      "Pizza Face",
      "Yellow Fellow",
      "Gobble Guy",
      "Circle Dude",
    ],
  },
  {
    question:
      "In 2012, a man legally changed his name to <BLANK> to get free food.",
    answer: "Bacon Double Cheeseburger",
    alternateSpellings: ["bacon double cheeseburger"],
    category: "food",
    suggestions: [
      "Free Pizza",
      "McDonald",
      "Happy Meal",
      "Papa John",
      "Taco Supreme",
      "Buffalo Wings",
      "Colonel Sanders",
      "Subway",
    ],
  },
  {
    question:
      "Astronauts on the International Space Station see <BLANK> sunrises every day.",
    answer: "16",
    alternateSpellings: ["sixteen", "16 sunrises"],
    category: "space",
    suggestions: ["2", "4", "8", "12", "24", "36", "48", "0"],
  },
  {
    question: "A single cloud can weigh more than <BLANK> pounds.",
    answer: "one million",
    alternateSpellings: ["1 million", "a million", "1000000"],
    category: "science",
    suggestions: [
      "one hundred",
      "ten thousand",
      "five hundred",
      "a billion",
      "one thousand",
      "fifty thousand",
      "ten million",
      "a trillion",
    ],
  },
  {
    question: "The dot over the letters 'i' and 'j' is called a <BLANK>.",
    answer: "tittle",
    alternateSpellings: ["a tittle"],
    category: "language",
    suggestions: ["pip", "dot", "jot", "nib", "speck", "dib", "blip", "cap"],
  },
  {
    question: "Bananas are technically classified as <BLANK>.",
    answer: "berries",
    alternateSpellings: ["a berry", "berry"],
    category: "science",
    suggestions: [
      "herbs",
      "vegetables",
      "grasses",
      "nuts",
      "flowers",
      "stems",
      "seeds",
      "legumes",
    ],
  },
  {
    question: "The Twitter bird's official name is <BLANK>.",
    answer: "Larry",
    alternateSpellings: ["Larry the bird", "Larry Bird"],
    category: "technology",
    suggestions: [
      "Tweety",
      "Chirp",
      "Blue",
      "Birdie",
      "Sky",
      "Tweet",
      "Pip",
      "Flappy",
    ],
  },
  {
    question:
      "In 2009, the country of Liechtenstein offered to rent itself out for <BLANK> a night.",
    answer: "$70,000",
    alternateSpellings: ["70000 dollars", "seventy thousand dollars", "70k"],
    category: "geography",
    suggestions: [
      "$1 million",
      "$500",
      "$10,000",
      "$250,000",
      "$5,000",
      "$1,000",
      "$50,000",
      "$100,000",
    ],
  },
  {
    question: "The Empire State Building has its own <BLANK>.",
    answer: "zip code",
    alternateSpellings: ["ZIP code", "postal code"],
    category: "facts",
    suggestions: [
      "fire department",
      "police force",
      "hospital",
      "school",
      "mayor",
      "cemetery",
      "currency",
      "anthem",
    ],
  },
  {
    question: "A jiffy is an actual unit of time equal to <BLANK>.",
    answer: "1/100th of a second",
    alternateSpellings: [
      "one hundredth of a second",
      "10 milliseconds",
      "0.01 seconds",
    ],
    category: "science",
    suggestions: [
      "1 millisecond",
      "3 seconds",
      "1 nanosecond",
      "half a second",
      "1/10th of a second",
      "5 microseconds",
      "a blink",
      "1/1000th of a second",
    ],
  },
  {
    question: "Before becoming an actor, Danny DeVito was a <BLANK>.",
    answer: "hairdresser",
    alternateSpellings: ["hair dresser", "beautician", "barber"],
    category: "celebrity",
    suggestions: [
      "plumber",
      "taxi driver",
      "butcher",
      "janitor",
      "wrestler",
      "clown",
      "chef",
      "mailman",
    ],
  },
  {
    question: "The first item sold on eBay was a <BLANK>.",
    answer: "broken laser pointer",
    alternateSpellings: ["broken laser", "a broken laser pointer"],
    category: "technology",
    suggestions: [
      "Beanie Baby",
      "comic book",
      "Pez dispenser",
      "used shoe",
      "baseball card",
      "toaster",
      "VHS tape",
      "empty box",
    ],
  },
  {
    question: "Cows have best friends and get <BLANK> when they are separated.",
    answer: "stressed",
    alternateSpellings: ["stressed out", "anxious", "upset", "sad"],
    category: "animals",
    suggestions: [
      "violent",
      "lost",
      "sick",
      "loud",
      "depressed",
      "confused",
      "lazy",
      "angry",
    ],
  },
  {
    question:
      "The coldest temperature ever recorded on Earth was <BLANK> degrees Fahrenheit.",
    answer: "-128",
    alternateSpellings: ["minus 128", "negative 128", "-128.6"],
    category: "science",
    suggestions: ["-89", "-200", "-45", "-175", "-300", "-62", "-150", "-100"],
  },
  {
    question: "In ancient Rome, urine was used as <BLANK>.",
    answer: "mouthwash",
    alternateSpellings: ["mouth wash", "a mouthwash", "teeth cleaner"],
    category: "history",
    suggestions: [
      "perfume",
      "paint thinner",
      "hair gel",
      "medicine",
      "ink",
      "fuel",
      "fertilizer",
      "soap",
    ],
  },
  {
    question:
      "The inventor of the fire hydrant is unknown because the patent was <BLANK>.",
    answer: "destroyed in a fire",
    alternateSpellings: ["lost in a fire", "burned in a fire"],
    category: "inventions",
    suggestions: [
      "stolen",
      "eaten by a dog",
      "never filed",
      "lost at sea",
      "classified",
      "written in invisible ink",
      "in a sunken ship",
      "misspelled",
    ],
  },
  {
    question: "There are more <BLANK> in the world than people.",
    answer: "chickens",
    alternateSpellings: ["chicken"],
    category: "animals",
    suggestions: [
      "rats",
      "trees",
      "ants",
      "cars",
      "phones",
      "Lego bricks",
      "grains of sand",
      "stars",
    ],
  },
  {
    question: "A crocodile cannot stick out its <BLANK>.",
    answer: "tongue",
    alternateSpellings: ["its tongue"],
    category: "animals",
    suggestions: [
      "tail",
      "legs",
      "arms",
      "eyes",
      "teeth",
      "claws",
      "belly",
      "neck",
    ],
  },
  {
    question:
      "The King of Hearts is the only king in a deck of cards without a <BLANK>.",
    answer: "mustache",
    alternateSpellings: ["moustache", "a mustache"],
    category: "facts",
    suggestions: [
      "crown",
      "sword",
      "beard",
      "nose",
      "scepter",
      "cape",
      "ring",
      "shield",
    ],
  },
  {
    question:
      "In 2016, someone paid $10,000 for a <BLANK> shaped like a dinosaur.",
    answer: "chicken nugget",
    alternateSpellings: ["Chicken McNugget", "McNugget", "nugget"],
    category: "food",
    suggestions: [
      "potato chip",
      "cloud",
      "rock",
      "cookie",
      "pancake",
      "pretzel",
      "piece of toast",
      "Dorito",
    ],
  },
  {
    question:
      "The inventor of the microwave oven discovered it when a <BLANK> melted in his pocket.",
    answer: "chocolate bar",
    alternateSpellings: ["candy bar", "chocolate"],
    category: "inventions",
    suggestions: [
      "crayon",
      "cheese stick",
      "stick of butter",
      "ice cube",
      "pen",
      "lipstick",
      "popsicle",
      "candle",
    ],
  },
  {
    question:
      "In 2015, a man in India set a world record by eating <BLANK> ghost peppers in one minute.",
    answer: "22",
    alternateSpellings: ["twenty-two", "twenty two"],
    category: "records",
    suggestions: ["10", "5", "50", "15", "35", "8", "100", "3"],
  },
  {
    question: "A flock of crows is called a <BLANK>.",
    answer: "murder",
    alternateSpellings: ["a murder"],
    category: "animals",
    suggestions: [
      "conspiracy",
      "darkness",
      "haunting",
      "shadow",
      "doom",
      "nightmare",
      "swarm",
      "mob",
    ],
  },
  {
    question:
      "In 2008, a couple was killed in South Africa while having sex on a <BLANK>.",
    answer: "railroad track",
    alternateSpellings: ["train track", "train tracks", "railway"],
    category: "news",
    suggestions: [
      "rooftop",
      "highway",
      "trampoline",
      "roller coaster",
      "diving board",
      "crane",
      "bridge",
      "waterfall",
    ],
  },
  {
    question:
      "The original Monopoly game was invented to demonstrate the evils of <BLANK>.",
    answer: "landlords",
    alternateSpellings: ["land ownership", "capitalism", "property ownership"],
    category: "gaming",
    suggestions: [
      "gambling",
      "greed",
      "banking",
      "taxes",
      "wealth",
      "corporations",
      "Wall Street",
      "cheating",
    ],
  },
  {
    question: "Ketchup was sold in the 1830s as <BLANK>.",
    answer: "medicine",
    alternateSpellings: ["a medicine", "medication"],
    category: "food",
    suggestions: [
      "paint",
      "perfume",
      "hair tonic",
      "cleaning fluid",
      "insect repellent",
      "lip balm",
      "cough syrup",
      "shoe polish",
    ],
  },
  {
    question:
      "The shortest complete sentence in the English language is '<BLANK>.'",
    answer: "I am",
    alternateSpellings: ["I am.", "Go"],
    category: "language",
    suggestions: ["No.", "Hi.", "Go.", "Do.", "Oh.", "So?", "Ha.", "OK."],
  },
  {
    question: "In zero gravity, a candle's flame is <BLANK> shaped.",
    answer: "sphere",
    alternateSpellings: ["spherical", "round", "ball"],
    category: "space",
    suggestions: [
      "flat",
      "spiral",
      "cone",
      "cube",
      "star",
      "ring",
      "pyramid",
      "invisible",
    ],
  },
];

// === Seeded PRNG ===

function seededRandom(seed) {
  var s = (seed + 0x6d2b79f5) | 0;
  var t = Math.imul(s ^ (s >>> 15), 1 | s);
  t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
  return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
}

function advanceSeed(seed) {
  return (seed + 0x6d2b79f5) | 0;
}

function deterministicPick(items, seed) {
  var r = seededRandom(seed);
  var idx = Math.floor(r * items.length);
  return { picked: items[idx], newSeed: advanceSeed(seed) };
}

function deterministicShuffle(arr, seed) {
  var result = arr.slice();
  var i, r, j, temp;
  for (i = result.length - 1; i > 0; i--) {
    r = seededRandom(seed);
    seed = advanceSeed(seed);
    j = Math.floor(r * (i + 1));
    temp = result[i];
    result[i] = result[j];
    result[j] = temp;
  }
  return { shuffled: result, newSeed: seed };
}

// === Helpers ===

function normalizeText(text) {
  return text
    .toLowerCase()
    .replace(/[^a-z0-9]/g, "")
    .trim();
}

function isDuplicateOfTruth(lie, question) {
  var normLie = normalizeText(lie);
  var normAnswer = normalizeText(question.answer);
  if (normLie === normAnswer) return true;
  var alts = question.alternateSpellings || [];
  var i;
  for (i = 0; i < alts.length; i++) {
    if (normLie === normalizeText(alts[i])) return true;
  }
  return false;
}

function pickQuestion(questionHistory, seed) {
  var available = [];
  var i, j;
  for (i = 0; i < QUESTIONS.length; i++) {
    if (questionHistory.indexOf(i) === -1) available.push(i);
  }
  if (available.length === 0) {
    // All used — reset and pick any
    available = [];
    for (j = 0; j < QUESTIONS.length; j++) available.push(j);
  }
  var result = deterministicPick(available, seed);
  return { questionIndex: result.picked, newSeed: result.newSeed };
}

function buildOptions(state) {
  var options = [];
  var players = state.players;
  var i, pid;

  // Add player lies
  for (i = 0; i < players.length; i++) {
    pid = players[i];
    if (state.lies[pid] !== undefined) {
      options.push({
        id: `lie_${pid}`,
        text: state.lies[pid],
        authorId: pid,
      });
    }
  }

  // Add truth
  options.push({
    id: "truth",
    text: state.currentQuestion.answer,
    authorId: null,
  });

  // Shuffle deterministically
  var result = deterministicShuffle(options, state.seed);
  return { options: result.shuffled, newSeed: result.newSeed };
}

function calculateRoundResults(state) {
  var pointsEarned = {};
  var players = state.players;
  var i, p, pid, votedFor, v, voter, optionId, lieAuthor;
  var l, d, vPid, vOptId, opt, o;
  for (i = 0; i < players.length; i++) {
    pointsEarned[players[i]] = 0;
  }

  // +500 for picking truth
  for (p = 0; p < players.length; p++) {
    pid = players[p];
    votedFor = state.votes[pid];
    if (votedFor === "truth") {
      pointsEarned[pid] += 500;
    }
  }

  // +250 for each player who picked your lie
  for (v = 0; v < players.length; v++) {
    voter = players[v];
    optionId = state.votes[voter];
    if (optionId && optionId !== "truth" && optionId.indexOf("lie_") === 0) {
      lieAuthor = optionId.replace("lie_", "");
      if (pointsEarned[lieAuthor] !== undefined) {
        pointsEarned[lieAuthor] += 250;
      }
    }
  }

  // Build detailed results
  var playerLies = {};
  for (l = 0; l < players.length; l++) {
    playerLies[players[l]] = state.lies[players[l]] || "";
  }

  var voteDetails = {};
  for (d = 0; d < players.length; d++) {
    vPid = players[d];
    vOptId = state.votes[vPid];
    if (vOptId) {
      opt = null;
      for (o = 0; o < state.options.length; o++) {
        if (state.options[o].id === vOptId) {
          opt = state.options[o];
          break;
        }
      }
      voteDetails[vPid] = {
        optionId: vOptId,
        text: opt ? opt.text : "",
        wasCorrect: vOptId === "truth",
      };
    }
  }

  return {
    truth: state.currentQuestion.answer,
    playerLies: playerLies,
    votes: voteDetails,
    pointsEarned: pointsEarned,
  };
}

function allPlayersSubmitted(state, field) {
  var players = state.players;
  var i;
  for (i = 0; i < players.length; i++) {
    if (!state[field][players[i]]) return false;
  }
  return true;
}

function startNextRound(state) {
  var qResult = pickQuestion(state.questionHistory, state.seed);
  var newHistory = state.questionHistory.concat([qResult.questionIndex]);

  return {
    phase: "writing",
    round: state.round + 1,
    maxRounds: state.maxRounds,
    players: state.players,
    scores: state.scores,
    currentQuestion: QUESTIONS[qResult.questionIndex],
    lies: {},
    hasWritten: {},
    options: [],
    votes: {},
    hasVoted: {},
    roundResults: null,
    questionHistory: newHistory,
    seed: qResult.newSeed,
    actionCount: state.actionCount,
  };
}

// === GameLogic ===

var GameLogic = {
  setup: (ctx) => {
    var playerIds = ctx.players.map((p) => p.id);
    var scores = {};
    var i;
    for (i = 0; i < playerIds.length; i++) {
      scores[playerIds[i]] = 0;
    }

    var maxRounds = ctx.config?.rounds || 5;
    var seed = ctx.seed || Math.floor(ctx.random.next() * 2147483647);
    var qResult = pickQuestion([], seed);

    return {
      phase: "writing",
      round: 1,
      maxRounds: maxRounds,
      players: playerIds,
      scores: scores,
      currentQuestion: QUESTIONS[qResult.questionIndex],
      lies: {},
      hasWritten: {},
      options: [],
      votes: {},
      hasVoted: {},
      roundResults: null,
      questionHistory: [qResult.questionIndex],
      seed: qResult.newSeed,
      actionCount: 0,
    };
  },

  actions: (state, playerId) => {
    var suggestions, playerIdx, suggestion, voteOptions, i, opt;

    if (state.phase === "writing") {
      if (state.players.indexOf(playerId) === -1) return [];
      if (state.hasWritten[playerId]) return [];
      // Return a template action with a suggestion. Platform structural matching
      // allows any string for the text field — players type their own lies.
      suggestions = state.currentQuestion.suggestions || [];
      playerIdx = state.players.indexOf(playerId);
      suggestion =
        suggestions.length > 0
          ? suggestions[playerIdx % suggestions.length]
          : "something funny";
      return [{ type: "write", text: suggestion }];
    }

    if (state.phase === "voting") {
      if (state.players.indexOf(playerId) === -1) return [];
      if (state.hasVoted[playerId]) return [];
      voteOptions = [];
      for (i = 0; i < state.options.length; i++) {
        opt = state.options[i];
        // Can't vote for own lie
        if (opt.authorId === playerId) continue;
        voteOptions.push({ type: "vote", optionId: opt.id });
      }
      return voteOptions;
    }

    // results and gameOver: no actions
    return [];
  },

  perform: (state, playerId, action) => {
    var newScores, s, pid, updatedState, lieText, newLies, newHasWritten, k, k2;
    var writeState,
      optResult,
      validOption,
      vi,
      newVotes,
      newHasVoted,
      vk,
      vk2,
      voteState;

    if (action.type === "finalize") {
      if (state.phase !== "gameOverDisplay") return state;
      return Object.assign({}, state, {
        phase: "gameOver",
        actionCount: state.actionCount + 1,
      });
    }

    if (action.type === "continue") {
      if (state.phase !== "results") return state;

      // Apply round results to scores
      newScores = {};
      for (s = 0; s < state.players.length; s++) {
        pid = state.players[s];
        newScores[pid] =
          (state.scores[pid] || 0) +
          (state.roundResults ? state.roundResults.pointsEarned[pid] || 0 : 0);
      }

      updatedState = {
        phase: state.phase,
        round: state.round,
        maxRounds: state.maxRounds,
        players: state.players,
        scores: newScores,
        currentQuestion: state.currentQuestion,
        lies: state.lies,
        hasWritten: state.hasWritten,
        options: state.options,
        votes: state.votes,
        hasVoted: state.hasVoted,
        roundResults: state.roundResults,
        questionHistory: state.questionHistory,
        seed: state.seed,
        actionCount: state.actionCount + 1,
      };

      if (state.round >= state.maxRounds) {
        return {
          phase: "gameOverDisplay",
          round: state.round,
          maxRounds: state.maxRounds,
          players: state.players,
          scores: newScores,
          currentQuestion: state.currentQuestion,
          lies: state.lies,
          hasWritten: state.hasWritten,
          options: state.options,
          votes: state.votes,
          hasVoted: state.hasVoted,
          roundResults: state.roundResults,
          questionHistory: state.questionHistory,
          seed: state.seed,
          actionCount: state.actionCount + 1,
        };
      }

      return startNextRound(updatedState);
    }

    // Player identity check
    if (state.players.indexOf(playerId) === -1) return state;

    if (action.type === "write" && state.phase === "writing") {
      if (state.hasWritten[playerId]) return state;

      lieText = (action.text || "").trim().slice(0, 45);
      if (!lieText) return state;

      // Don't allow the exact truth
      if (isDuplicateOfTruth(lieText, state.currentQuestion)) return state;

      newLies = {};
      newHasWritten = {};
      for (k in state.lies) newLies[k] = state.lies[k];
      for (k2 in state.hasWritten) newHasWritten[k2] = state.hasWritten[k2];
      newLies[playerId] = lieText;
      newHasWritten[playerId] = true;

      writeState = {
        phase: state.phase,
        round: state.round,
        maxRounds: state.maxRounds,
        players: state.players,
        scores: state.scores,
        currentQuestion: state.currentQuestion,
        lies: newLies,
        hasWritten: newHasWritten,
        options: state.options,
        votes: state.votes,
        hasVoted: state.hasVoted,
        roundResults: state.roundResults,
        questionHistory: state.questionHistory,
        seed: state.seed,
        actionCount: state.actionCount + 1,
      };

      // Check if all players submitted
      if (allPlayersSubmitted(writeState, "hasWritten")) {
        optResult = buildOptions(writeState);
        writeState.options = optResult.options;
        writeState.seed = optResult.newSeed;
        writeState.phase = "voting";
      }

      return writeState;
    }

    if (action.type === "vote" && state.phase === "voting") {
      if (state.hasVoted[playerId]) return state;

      // Validate option exists and isn't own lie
      validOption = null;
      for (vi = 0; vi < state.options.length; vi++) {
        if (state.options[vi].id === action.optionId) {
          validOption = state.options[vi];
          break;
        }
      }
      if (!validOption) return state;
      if (validOption.authorId === playerId) return state;

      newVotes = {};
      newHasVoted = {};
      for (vk in state.votes) newVotes[vk] = state.votes[vk];
      for (vk2 in state.hasVoted) newHasVoted[vk2] = state.hasVoted[vk2];
      newVotes[playerId] = action.optionId;
      newHasVoted[playerId] = true;

      voteState = {
        phase: state.phase,
        round: state.round,
        maxRounds: state.maxRounds,
        players: state.players,
        scores: state.scores,
        currentQuestion: state.currentQuestion,
        lies: state.lies,
        hasWritten: state.hasWritten,
        options: state.options,
        votes: newVotes,
        hasVoted: newHasVoted,
        roundResults: state.roundResults,
        questionHistory: state.questionHistory,
        seed: state.seed,
        actionCount: state.actionCount + 1,
      };

      // Check if all players voted
      if (allPlayersSubmitted(voteState, "hasVoted")) {
        voteState.roundResults = calculateRoundResults(voteState);
        voteState.phase = "results";
      }

      return voteState;
    }

    return state;
  },

  view: (state, playerId) => {
    if (playerId === null) {
      // Spectator view
      return {
        phase: state.phase,
        round: state.round,
        maxRounds: state.maxRounds,
        scores: state.scores,
        currentQuestion: {
          question: state.currentQuestion.question,
          category: state.currentQuestion.category,
        },
        hasWritten: state.hasWritten,
        hasVoted: state.hasVoted,
        options:
          state.phase === "voting" ||
          state.phase === "results" ||
          state.phase === "gameOverDisplay" ||
          state.phase === "gameOver"
            ? state.options.map((o) => ({ id: o.id, text: o.text }))
            : [],
        roundResults:
          state.phase === "results" ||
          state.phase === "gameOverDisplay" ||
          state.phase === "gameOver"
            ? state.roundResults
            : null,
        playerCount: state.players.length,
      };
    }

    var base = {
      phase: state.phase,
      round: state.round,
      maxRounds: state.maxRounds,
      scores: state.scores,
      currentQuestion: {
        question: state.currentQuestion.question,
        category: state.currentQuestion.category,
      },
      players: state.players,
      hasWritten: state.hasWritten,
      hasVoted: state.hasVoted,
    };

    if (state.phase === "writing") {
      // Show own lie if submitted, hide others
      base.myLie = state.lies[playerId] || null;
      base.options = [];
      base.votes = {};
      base.roundResults = null;
    } else if (state.phase === "voting") {
      base.myLie = state.lies[playerId] || null;
      // Show options without revealing authorship
      base.options = state.options.map((o) => ({
        id: o.id,
        text: o.text,
        isMine: o.authorId === playerId,
      }));
      base.votes = {};
      base.myVote = state.votes[playerId] || null;
      base.roundResults = null;
    } else if (
      state.phase === "results" ||
      state.phase === "gameOverDisplay" ||
      state.phase === "gameOver"
    ) {
      base.myLie = state.lies[playerId] || null;
      base.options = state.options.map((o) => ({
        id: o.id,
        text: o.text,
        authorId: o.authorId,
        isTruth: o.id === "truth",
      }));
      base.roundResults = state.roundResults;
      base.myVote = state.votes[playerId] || null;
    }

    return base;
  },

  isOver: (state) => {
    if (state.phase !== "gameOver") return null;

    var players = state.players;
    var maxScore = 0;
    var i, j;
    for (i = 0; i < players.length; i++) {
      if (state.scores[players[i]] > maxScore)
        maxScore = state.scores[players[i]];
    }

    var winners = [];
    for (j = 0; j < players.length; j++) {
      if (state.scores[players[j]] === maxScore) winners.push(players[j]);
    }

    var summary =
      winners.length === 1
        ? `Winner with ${maxScore} points!`
        : `Tie at ${maxScore} points!`;

    return { winners: winners, summary: summary };
  },

  turnConfig: (state, playerId) => {
    var suggestion, firstOption;

    if (state.phase === "writing") {
      if (playerId === null) {
        // System timer: auto-submit random suggestion for all unsubmitted
        suggestion =
          state.currentQuestion.suggestions &&
          state.currentQuestion.suggestions.length > 0
            ? state.currentQuestion.suggestions[0]
            : "something funny";
        return {
          timeoutMs: 60000,
          defaultAction: { type: "write", text: suggestion },
        };
      }
      return { timeoutMs: 60000 };
    }

    if (state.phase === "voting") {
      if (playerId === null) {
        // System timer: auto-vote for first non-own option
        firstOption = state.options.length > 0 ? state.options[0].id : "truth";
        return {
          timeoutMs: 30000,
          defaultAction: { type: "vote", optionId: firstOption },
        };
      }
      return { timeoutMs: 30000 };
    }

    if (state.phase === "results") {
      if (playerId === null) {
        return {
          timeoutMs: 8000,
          defaultAction: { type: "continue" },
        };
      }
      return { timeoutMs: 8000 };
    }

    if (state.phase === "gameOverDisplay") {
      if (playerId === null) {
        return { timeoutMs: 5000, defaultAction: { type: "finalize" } };
      }
      return { timeoutMs: 5000 };
    }

    if (state.phase === "gameOver") {
      return {};
    }

    return {};
  },
};

// Export for tests — the platform loader strips this
export default GameLogic;
